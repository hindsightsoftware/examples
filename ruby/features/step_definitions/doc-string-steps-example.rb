Given("I have filled in the address details form") do
  # Given glue code
end

When("I submit the form") do
  # When glue code
end

Then("the address is processed and I receive the message") do | docString |
  puts docString
end